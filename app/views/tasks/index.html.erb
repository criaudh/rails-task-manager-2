<h1>My tasks</h1>
<ul>
  <% @tasks.each do |task| %>
    <li><%= task.title %></li>
    <%= link_to "Details", task_path(task.id) %>
  <% end %>
</ul>
<%= link_to "Add a new task", new_task_path, class: "btn btn-primary" %>
